export const getTeamMembers = () => {
  // This function is no longer used, data is fetched from Supabase
  return [];
};

export const saveTeamMembers = (members) => {
  // This function is no longer used, data is saved to Supabase
};